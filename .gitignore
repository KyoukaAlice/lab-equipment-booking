# ---------------------------------------------------------------------
# 运行时数据：默认数据库与 WAL 日志不入库
# 首次启动会自动建库并生成演示数据（见 src/seed/seed-data.js），
# 因此仓库里不需要携带 lab.db。
# ---------------------------------------------------------------------
data/
*.db
*.db-wal
*.db-shm

# ---------------------------------------------------------------------
# 依赖与依赖锁（本项目运行时零第三方依赖，正常不会产生 node_modules）
# ---------------------------------------------------------------------
node_modules/
package-lock.json

# ---------------------------------------------------------------------
# 日志与临时文件
# ---------------------------------------------------------------------
*.log
npm-debug.log*
tmp/
temp/
*.tmp
*.bak

# 本地环境变量（如需自定义 PORT / LAB_DB_PATH，请写在 .env.local）
.env
.env.local

# ---------------------------------------------------------------------
# 编辑器与系统
# ---------------------------------------------------------------------
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------
# 截图脚本的中间产物（正式截图位于 docs/images/，需入库）
# ---------------------------------------------------------------------
docs/images/_*.png
